<!DOCTYPE html>
<html>

<head>
    <title>Google Auth App</title>
</head>

<body>
    <% if (user) { %>
        <p>User Info: <%= user %>
        </p>
        <a href="/logout">Logout</a>
        <% } else { %>
            <h1>Welcome!</h1>
            <a href="/auth/google">Login with Google</a>
            <% } %>
</body>

</html>